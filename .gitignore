__pycache__/
*.pyc
.pytest_cache/
.venv/
.env
# 运行时数据目录（SQLite + PDF + 图片）。**必须锚定根目录**：
# 写成 `data/` 会连 tests/data/ 里的测试夹具一起忽略掉
# —— 本地测不出来，CI 上直接 FileNotFoundError（已踩）。
/data/
out/
dist/
build/
*.egg-info/
.DS_Store

# 前端构建产物（由 `cd web && npm run build` 生成，不入库）
src/papershelf/static/assets/
src/papershelf/static/index.html

# 前端依赖
web/node_modules/
web/dist/

# ── EMRG agent 运行时（与姊妹项目一致） ──────────────────────────
# 不提交：运行日志、演化产物、LLM 原始记录、会话临时文件
.emrg/emrg-*.log
.emrg/emrg-*.log.*
.emrg/emrgd.*
.emrg/evolution/
.emrg/saturation/
.emrg/sessions/**/llm.jsonl
.emrg/sessions/**/llm.jsonl.*
.emrg/sessions/**/tmp/
# 保留 .emrg/memory/（项目记忆）与 .emrg/sessions/**/{memory,meta.json,system.md,history*.jsonl,images}
